version: "3.9"

services:
  heart-disease-api:
    build:
      context: ..
      dockerfile: Dockerfile
    image: heart-disease-api:latest
    container_name: heart-disease-api
    ports:
      - "8000:8000"
    volumes:
      - ../api_requests.log:/app/src/api_requests.log
    restart: unless-stopped

  # Optional: Prometheus for metrics scraping (Task 8 - Monitoring)
  prometheus:
    image: prom/prometheus:latest
    container_name: prometheus
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml
    ports:
      - "9090:9090"
    depends_on:
      - heart-disease-api

  # Optional: Grafana for dashboarding (Task 8 - Monitoring)
  grafana:
    image: grafana/grafana:latest
    container_name: grafana
    ports:
      - "3000:3000"
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=admin
    depends_on:
      - prometheus
